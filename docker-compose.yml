version: '3.8'
services:
  # Redis for Rate Limiting & Caching
  redis:
    image: redis:alpine
    container_name: paywatch-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
